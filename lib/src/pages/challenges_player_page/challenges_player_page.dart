import 'package:flutter/material.dart';

class ChallangesPlayerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Player challenge page"),);
  }
}